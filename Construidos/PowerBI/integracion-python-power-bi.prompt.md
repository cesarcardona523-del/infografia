# Especificación de Diseño — Infografías Técnicas

> Ver INFOGRAFIA-SPEC.md para el detalle completo (composición editorial, sin fotos/escenas/personas, firma LinkedIn + `cacm523`, 1200×627px).

---

## Contexto específico — Conecta Power BI con Python: Pronóstico de Datos

**Referencia**: `referencias/PowerBI/integracion-python-power-bi/PowerBI_Python.jpeg` (Data & Analytics) — flujo de 4 pasos para integrar Python dentro de Power BI y generar un pronóstico de ventas, con un ejemplo real de resultado (gráfico Actual vs. Predicción, ventas pronosticadas $1.2M). Se conserva la estructura completa de 4 pasos y el ejemplo numérico real, redibujando el editor de código y el gráfico como elementos propios (no capturas de pantalla).

**Los 4 pasos (contenido real)**:
1. Importar y Preparar Datos en Power BI — cargar datos históricos (ej. ventas mensuales), establecer los tipos de datos correctos.
2. Desarrollo del Script de Python — modelado con librerías (Pandas, Prophet, Scikit-learn), configurar el entorno de Python en las opciones de Power BI.
3. Ejecución del Modelo y Tratamientos — invocación de la función predictiva, generación de predicciones, cálculo de métricas de precisión, tratamiento de nulos y valores atípicos.
4. Visualización Avanzada en Power BI — pronóstico de ventas (histórico vs. predicción) con un intervalo de confianza del 95%.

**Ejemplo numérico real**: gráfico de 12 meses con ventas históricas (Actual) y predicción futura (Forecast, banda de confianza del 95%); ventas pronosticadas totales: **$1.2M**.

**Dirección visual**: paleta dual de marca real — dorado de Power BI (`#F2C811`) + azul/amarillo de Python (`#306998` / `#FFD43B`) — flujo horizontal de 4 pasos con flechas, gráfico de línea real (histórico + banda de predicción) como elemento central en vez de captura de pantalla.
